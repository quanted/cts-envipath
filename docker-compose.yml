
services:
  envipath:
    restart: unless-stopped
    image: ghcr.io/quanted/cts-envipath:dev
    container_name: cts-envipath
    ports:
      - "5003:5003"
    environment:
      - HOSTNAME=docker
    env_file:
      - ./.env
